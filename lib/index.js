'use strict';

let FileSystem = require('fs');
let Path = require('path');

/**
 * Transmuter
 * @public
 * @class
 */
class Transmuter {

  /**
   * Instantiates a Transmuter
   * @param {String} relativePath - a relative path to the keys
   * @param {Object} [options]
   */
  constructor(key, options) {
    options = options || {};
  }


}

module.exports = Transmuter;